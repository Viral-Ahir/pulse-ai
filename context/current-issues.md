Issue 1 - So when we are typing project name in the dialog box when clicking on the new project, the font comes in dark color, and so its not visible with the dark background. I want that with light color font, so it visible. Same with the rename project dialog.

Issue 2 - When clicking on create project button, it does not create the project in the project sidebar UI, and also the same with rename, it does not rename, and delete does not delete from the sidebar UI.
